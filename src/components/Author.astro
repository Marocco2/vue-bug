---
export interface Props {
	name: string;
	href: string;
}

const { name, href } = Astro.props;
---
<a class="author" {href}>Di: {name}</a>


<style>
	.author {
		margin-bottom: 0.75rem;
		color: var(--theme-text-lighter);
	}
</style>
